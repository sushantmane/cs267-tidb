
class BooleanRetrieval {

    /** boolean retrieval returns a sets of documents (rather than ranked list).
     * A term t is considered to specify the set of documents containing it.
     * The standard boolean operators AND, OR, and NOT are used to construct boolean queries.
     * These boolean queries are interpreted as operations over these sets.
     */
}


public class RankCosine {

    // Query processing for ranked retrieval under vector space model
    public void rankCosine(String[] termVector, int k) {
        int j = 0;
//        int docId = nextDoc();

    }

    public static void main() {


    }
}
